apiVersion: v2
name: cert-manager
description: >
  cert-manager — automatic TLS certificate lifecycle management.
  Provisions and auto-renews certificates from Let's Encrypt (ACME)
  using DNS-01 (Cloudflare) and HTTP-01 (nginx ingress) solvers.
type: application
version: 1.0.0
appVersion: "v1.15.3"
dependencies:
  - name: cert-manager
    version: "v1.15.3"
    repository: "https://charts.jetstack.io"
